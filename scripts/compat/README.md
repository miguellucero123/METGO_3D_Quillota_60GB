# Scripts de compatibilidad

Wrappers que reexportan módulos en `backend/` o `frontend/dashboards/`.

Se añaden al `sys.path` al llamar `metgo.streamlit_bootstrap.bootstrap()`.

No colocar en la raíz del repo (GitHub se ve más ordenado).
