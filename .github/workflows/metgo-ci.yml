# Redirigido a ci.yml (mantener compatibilidad con enlaces antiguos)
name: METGO CI (legacy alias)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/ci.yml"
