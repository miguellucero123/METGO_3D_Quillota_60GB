# Cloudflare Pages — METGO3D (SPA Vue)
# Dashboard: crear proyecto "metgo-quillota" con root = frontend/vue
# Seguridad: scripts/ops/CLOUDFLARE_PAGES_HARDENING.md
#   → Settings → Builds & deployments → Preview deployments = None (o solo PRs)
#   → Custom domain + WAF cuando DNS esté en Cloudflare
#
# CLI local (opcional):
#   npm run build
#   npx wrangler pages deploy dist --project-name=metgo-quillota

name = "metgo-quillota"
compatibility_date = "2026-07-01"
pages_build_output_dir = "dist"
