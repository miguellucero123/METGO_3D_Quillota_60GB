# Copiar a .env.production (local, no se sube a Git)
# Netlify: no hace falta variable si usa proxy /api en public/_redirects
# Opcional en Netlify UI (no marcar como "secret"): VITE_METGO_API=/api

VITE_METGO_API=/api
VITE_API_PORT=8080
