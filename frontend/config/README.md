# Configuración frontend (reservado)

Aquí irá configuración compartida entre Vue y Streamlit (temas, URLs, feature flags).

Hoy la configuración activa está en:

- `frontend/vue/.env.development`
- Raíz del repo: `.env.example`
