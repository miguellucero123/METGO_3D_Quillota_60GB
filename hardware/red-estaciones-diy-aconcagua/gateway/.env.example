# Copiar a .env y cambiar secretos tras el primer `docker compose up`
POSTGRES_PASSWORD=chirpstack
INFLUX_USER=metgo
INFLUX_PASSWORD=metgochangeme
INFLUX_ORG=metgo
INFLUX_BUCKET=estaciones
INFLUX_TOKEN=change-me-after-first-boot
GRAFANA_PASSWORD=admin

# Opcional: reenvío a METGO (usa integration/metgo_ingest.py)
METGO_API_URL=https://metgo-api.onrender.com/api
METGO_JWT=
METGO_INGEST_ENABLE=0
