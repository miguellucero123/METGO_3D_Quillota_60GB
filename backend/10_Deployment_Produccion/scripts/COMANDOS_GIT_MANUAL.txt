================================================================================
  METGO — Subida a GitHub MANUAL (copiar y pegar)
  Proyecto: D:\METGO_3D_Quillota_60GB
  Guia: docs\manuales\SUBIR_GITHUB_MANUAL.md
================================================================================

cd /d D:\METGO_3D_Quillota_60GB

git status -sb
git remote -v

git add -A
git reset HEAD .env
git reset HEAD .env.local
git reset HEAD .streamlit\secrets.toml

git diff --cached --name-status
git diff --cached --shortstat

git commit -m "feat: Visor de puertos integrado, utilidad por modulo y despliegue nube"

git branch --show-current
git push origin master

--- Si la rama es main en lugar de master:
git push origin main

--- Si GitHub pide actualizar antes:
git pull --rebase origin master
git push origin master

--- Primera vez en la rama:
git push -u origin master

================================================================================
